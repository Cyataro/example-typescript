import Sample from '../../src/lib/sample'
import assert from 'power-assert'

describe('Sample', () => {
  describe('constractor', () => {
    it('', () => {
      assert(new Sample({name: 'test'}))
    })
  })
})
